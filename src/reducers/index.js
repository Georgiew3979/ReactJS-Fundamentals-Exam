import { registerReducer, loginReducer } from './authReducers'
import { yearlyReducer, monthlyReducer } from './yearlyReducers'

export default {
  registerReducer,
  loginReducer,
  yearlyReducer,
  monthlyReducer
}
